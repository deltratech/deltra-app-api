export enum AnnouncementAudienceType {
  school = 'school',
  grade = 'grade',
  class = 'class',
}

export enum AnnouncementChannel {
  in_app = 'in_app',
  whatsapp = 'whatsapp',
  email = 'email',
}

export enum AnnouncementStatus {
  draft = 'draft',
  scheduled = 'scheduled',
  sent = 'sent',
  cancelled = 'cancelled',
}

export enum AnnouncementTemplateCategory {
  school_holiday = 'school_holiday',
  parent_meeting = 'parent_meeting',
  exam = 'exam',
}
